<?xml version="1.0" encoding="utf-8"?>
<set xmlns:android="http://schemas.android.com/apk/res/android"
    android:fillAfter="true" android:duration="500">
    <scale android:fromYScale="@dimen/from_scale" android:fromXScale="@dimen/from_scale"
        android:toYScale="@dimen/to_scale" android:toXScale="@dimen/to_scale"/>
    <scale android:fromXScale="@dimen/to_scale" android:fromYScale="@dimen/to_scale"
        android:toXScale="@dimen/from_scale" android:toYScale="@dimen/from_scale"/>

</set>